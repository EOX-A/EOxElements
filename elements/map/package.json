{
  "name": "@eox/map",
  "version": "1.15.0",
  "type": "module",
  "dependencies": {
    "flatgeobuf": "^3.35.0",
    "lit": "^3.0.2",
    "ol": "^10.2.0",
    "ol-stac": "^1.0.0-beta.10",
    "proj4": "^2.9.2"
  },
  "devDependencies": {
    "@types/proj4": "^2.5.4",
    "vite": "^5.0.2"
  },
  "engines": {
    "npm": ">=8.0.0",
    "node": ">=18.0.0"
  },
  "files": [
    "dist",
    "test"
  ],
  "main": "./dist/eox-map.js",
  "scripts": {
    "build": "tsc && vite build --config vite.config.advancedLayersAndSources.js && vite build --config vite.config.js",
    "watch": "tsc && vite build --config vite.config.advancedLayersAndSources.js && vite build --config vite.config.js --watch"
  }
}
