describe("Layer config", () => {
  beforeEach(() => {
    cy.visit(
      "http://localhost:6006/iframe.html?id=elements-eox-layercontrol--layer-config"
    );
  });
  it("Should show layercontrol", () => {});
});
